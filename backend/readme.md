in the root directory of the repository, </br>
> first, run `npm i` </br>
> second, run `npm run build` </br>
</br>
> to connect to our mongodb instance (via mongoose), </br>
> `rm  -fr backend/build`</br>
</br>
> `mv build backend/` </br>
</br>
> `serve -s backend/build &` </br>
> `cd backend && node server.js &` </br>

